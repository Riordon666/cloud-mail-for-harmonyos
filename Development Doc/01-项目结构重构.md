# 01 — 项目结构重构

> **前置条件**：无
> **目标**：拆分 `Index.ets`（276 行单文件）→ 独立页面文件 + 可复用组件

## 当前问题

`Index.ets` 包含 7 个 `@Component`（Index + InboxView + StarredView + ComposeView + AccountsView + MeView + EmailCard + AccountCard），全部在一个文件中。难以维护、无法子页面导航。

## 目标结构

```
mail/src/main/ets/
├── common/
│   ├── Constants.ets          ✅ 已有（不动）
│   └── MockData.ets           ⏳ 新建（集中管理 mock 数据）
├── components/
│   ├── EmailCard.ets          ⏳ 从 Index.ets 提取
│   ├── AccountCard.ets        ⏳ 从 Index.ets 提取
│   └── AvatarBar.ets          ⏳ 新建（头像 + 首字母）
├── pages/
│   ├── Index.ets              🔧 重构（仅保留 HdsNavigation 框架）
│   ├── InboxPage.ets          ⏳ 从 Index.ets 提取
│   ├── StarredPage.ets        ⏳ 从 Index.ets 提取
│   ├── ComposePage.ets        ⏳ 从 Index.ets 提取
│   ├── AccountsPage.ets       ⏳ 从 Index.ets 提取
│   └── MePage.ets             ⏳ 从 Index.ets 提取
├── mailability/
│   └── MailAbility.ets        ✅ 已有（不动）
└── mailbackupability/
    └── MailBackupAbility.ets  ✅ 已有（不动）
```

## 任务清单

### 任务 1：创建 MockData.ets
- [ ] 提取 `InboxView` 中硬编码的 6 封邮件数据
- [ ] 提取 `AccountsView` 中硬编码的 3 个账号数据
- [ ] 统一导出为 `MOCK_EMAILS: EmailMock[]` 和 `MOCK_ACCOUNTS: AccountMock[]`
- [ ] 定义 `EmailMock` 和 `AccountMock` 接口

### 任务 2：提取 AvatarBar 组件
- [ ] 把头像渲染逻辑（Circle + 首字母 + 哈希取色）提取为独立组件
- [ ] Props: `name: string`, `size?: number`
- [ ] 文件：`components/AvatarBar.ets`

### 任务 3：提取 EmailCard 组件
- [ ] 把 `InboxView.EmailCard()` 提取为独立组件
- [ ] Props: `email: EmailMock`
- [ ] 事件: `onClick?: () => void`
- [ ] 文件：`components/EmailCard.ets`

### 任务 4：提取 AccountCard 组件
- [ ] 把 `AccountsView.AccountCard()` 提取为独立组件
- [ ] Props: `account: AccountMock`
- [ ] 文件：`components/AccountCard.ets`

### 任务 5：拆分页面文件
- [ ] `pages/InboxPage.ets` — 收件箱（搜索栏 + List<EmailCard>）
- [ ] `pages/StarredPage.ets` — 星标邮件（空状态 + List）
- [ ] `pages/ComposePage.ets` — 写邮件（三个输入区）
- [ ] `pages/AccountsPage.ets` — 账号管理（List<AccountCard>）
- [ ] `pages/MePage.ets` — 个人中心（头像 + 菜单 List）

### 任务 6：精简 Index.ets
- [ ] 只保留 HdsNavigation + HdsTabs 框架
- [ ] 导入各页面组件
- [ ] `@Builder TabBarItem` 保留在 Index.ets
- [ ] 修改 Tab 图标为 SymbolGlyph（收件箱→envelope, 星标→star, 写邮件→square.and.pencil, 账号→person.2, 我→person.crop.circle）

### 任务 7：验证
- [ ] `hvigor assembleHap` 编译通过
- [ ] 模拟器运行，5 个 Tab 正常显示和切换
- [ ] 所有 mock 数据正常展示

## 验收标准

- ✅ Index.ets 不超过 80 行（仅框架）
- ✅ 每个页面组件在独立文件中
- ✅ MockData.ets 统一管理所有 mock 数据
- ✅ 编译无报错
- ✅ 运行效果与重构前一致
