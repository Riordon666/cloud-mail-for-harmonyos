# 02 — 完善设计系统

> **前置条件**：01 项目结构重构完成
> **目标**：统一设计 Token、补全色板、建立 Mock 数据中心、封装通用组件

## 任务清单

### 任务 1：补全 Constants.ets
- [ ] 补充缺失 Token：`FONT_SIZE_HERO`(28), `FONT_SIZE_CAPTION`(10)
- [ ] 补充间距：`PADDING_CARD`(12), `GAP_SMALL`(8), `GAP_MEDIUM`(12)
- [ ] 补充阴影：`SHADOW_CARD`, `SHADOW_FLOAT`
- [ ] 补充动画时长：`DURATION_FAST`(150), `DURATION_NORMAL`(300)

### 任务 2：补全浅色/深色色板
- [ ] 补充 `warning`（橙 #FF9500）、`info`（青 #5AC8FA）
- [ ] 补充 `primary_dark`（深蓝 #1A56DB）、`text_link`（链接蓝）
- [ ] 补充 `overlay`（遮罩黑 rgba(0,0,0,0.4)）
- [ ] 补充 `skeleton`（骨架屏灰 #E8E8E8 / #2C2C2E）
- [ ] 更新 `resources/base/element/color.json` 和 `resources/dark/element/color.json`

### 任务 3：MockData.ets 数据中心
- [ ] 迁移 6 封 mock 邮件（丰富字段：cc, bcc, attList, htmlContent）
- [ ] 迁移 3 个 mock 账号
- [ ] 新增 mock 用户数据（admin + 普通用户）
- [ ] 新增 mock 设置数据（网站标题、注册开关等）
- [ ] 新增 mock 管理员数据（角色列表、权限树、统计数据）
- [ ] 新增 mock 注册码数据
- [ ] 所有数据标注类型（对照原项目 entity 结构）

### 任务 4：通用组件封装
- [ ] `EmptyState.ets` — 空状态（图标 + 文字，如"暂无邮件"）
- [ ] `LoadingView.ets` — 加载中（骨架屏或 LoadingProgress）
- [ ] `Badge.ets` — 未读角标（数字或红点）
- [ ] `SectionHeader.ets` — 分组标题（设置页通用）

### 任务 5：Tab 图标更新
- [ ] 收件箱 Tab：`sys.symbol.envelope_fill` / `sys.symbol.envelope`
- [ ] 星标 Tab：`sys.symbol.star_fill` / `sys.symbol.star`
- [ ] 写邮件 Tab：`sys.symbol.square_and_pencil`
- [ ] 账号 Tab：`sys.symbol.person_2_fill` / `sys.symbol.person_2`
- [ ] 我 Tab：`sys.symbol.person_crop_circle_fill` / `sys.symbol.person_crop_circle`
- [ ] 每个图标选中态用 `_fill` 版本 + primary 色，非选中态用无色版 + tertiary 色

## 验收标准
- ✅ 编译通过，运行效果与重构前一致
- ✅ 色板补全，深色模式色值完整
- ✅ MockData 覆盖所有页面需要的假数据
- ✅ 4 个通用组件可用
